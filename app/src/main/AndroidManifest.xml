<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devs.mapdrawingmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--This google map key is restricted with developer SHA1, It will not work for you,
         so please crete new one for your test -->
        <meta-data android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBXsFGkFo1sUxNZI-n2Zggqsg-zEyJ2Pko"/>


    </application>

</manifest>